class Solution {
public:
    bool isPalindrome(int x) {
        string a = to_string(x);
        reverse(a.begin(),a.end());
        string b=to_string(x);
        if (a==b) return true;
        return false;
    }
};
